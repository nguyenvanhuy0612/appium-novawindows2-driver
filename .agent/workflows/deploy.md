---
description: Build and deploy the driver to the Windows share.
---
This workflow builds the Appium driver locally and syncs the artifacts to the mounted Windows share (~/win_share).

1. Run the deployment script.
// turbo
sh ./deploy.sh
